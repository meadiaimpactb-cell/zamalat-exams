-- ============================================================================
-- ترحيل: صورة البروفايل الشخصية للمستخدمين
-- طبّقه مرة واحدة على قاعدة الإنتاج: phpMyAdmin > قاعدة u153052345_zamalat > SQL
-- آمن للتكرار (IF NOT EXISTS)
-- ============================================================================

ALTER TABLE `users`
  ADD COLUMN IF NOT EXISTS `avatarUrl` MEDIUMTEXT NULL AFTER `phone`;
