/* ============================================================================
 * مفاتيح التهيئة (Feature flags) — مشتركة بين العميل والخادم
 * ========================================================================== */

export const FEATURES = {
  /**
   * استعلام الطالب عن نتيجته داخل منصة الاختبارات (ملاحظة 3).
   * معطَّل: النتائج تُنشر عبر موقع الزمالة الرئيسي فقط لتوحيد قناة الإعلان.
   * الكود كامل خلف هذا المفتاح — لإعادة التفعيل اجعله true وأعد البناء.
   */
  candidateResultLookup: false,
} as const;
